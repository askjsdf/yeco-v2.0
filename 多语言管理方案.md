# YECO 全站多语言管理方案

## 📋 方案概述

本方案针对 YECO 网站的两种内容类型,设计了统一且灵活的多语言管理架构:

- **静态内容**: 首页、关于页等不常变动的页面
- **动态内容**: 报告列表、报告详情等持续更新的内容

**设计原则**:
- 零依赖的轻量级方案,无需安装额外库
- 统一的语言切换机制,全站一致的用户体验
- 渐进式翻译策略,优先翻译高价值内容
- 符合 YECO 极简主义设计理念

---

## 🏗️ 架构设计

### 1. 核心语言管理层

```
src/
├── i18n/
│   ├── config.ts                 # 语言配置
│   ├── LanguageContext.tsx       # 语言状态管理
│   ├── dictionaries/             # 翻译字典
│   │   ├── zh.ts                 # 中文
│   │   ├── en.ts                 # 英文
│   │   └── index.ts              # 导出
│   └── hooks/
│       └── useTranslation.ts     # 翻译钩子
```

### 2. 内容分层管理

#### 静态内容翻译
存储在字典文件中,手动维护高质量翻译

#### 动态内容翻译
- **元数据**: reports-data.ts 扩展多语言字段
- **报告正文**: 每个报告组件内置多语言版本

---

## 📝 实施步骤

### 第一阶段:搭建基础设施 (30分钟)

#### Step 1: 创建语言配置

**文件**: `src/i18n/config.ts`

```typescript
// 支持的语言列表
export const languages = ['zh', 'en'] as const;
export type Language = (typeof languages)[number];

// 默认语言
export const defaultLanguage: Language = 'zh';

// 语言显示名称(用于语言切换器)
export const languageNames: Record<Language, string> = {
  zh: '中文',
  en: 'English',
};
```

#### Step 2: 创建语言上下文

**文件**: `src/i18n/LanguageContext.tsx`

```typescript
'use client';

import { createContext, useContext, useState, useEffect, ReactNode } from 'react';
import { Language, defaultLanguage } from './config';

interface LanguageContextType {
  language: Language;
  setLanguage: (lang: Language) => void;
}

const LanguageContext = createContext<LanguageContextType | undefined>(undefined);

export function LanguageProvider({ children }: { children: ReactNode }) {
  const [language, setLanguageState] = useState<Language>(defaultLanguage);

  // 从 localStorage 读取语言偏好
  useEffect(() => {
    const saved = localStorage.getItem('yeco-language') as Language;
    if (saved) setLanguageState(saved);
  }, []);

  // 保存语言偏好到 localStorage
  const setLanguage = (lang: Language) => {
    setLanguageState(lang);
    localStorage.setItem('yeco-language', lang);
    document.documentElement.lang = lang;
  };

  return (
    <LanguageContext.Provider value={{ language, setLanguage }}>
      {children}
    </LanguageContext.Provider>
  );
}

export function useLanguage() {
  const context = useContext(LanguageContext);
  if (!context) throw new Error('useLanguage must be used within LanguageProvider');
  return context;
}
```

#### Step 3: 集成到根布局

**文件**: `src/app/layout.tsx`

在现有的 ThemeProvider 和 VideoProvider 之外,添加 LanguageProvider:

```typescript
import { LanguageProvider } from "@/i18n/LanguageContext";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh" suppressHydrationWarning={true}>
      <body className={/* ... */}>
        <LanguageProvider>
          <ThemeProvider defaultTheme="dark" forcedTheme="dark">
            <VideoProvider>
              {children}
            </VideoProvider>
          </ThemeProvider>
        </LanguageProvider>
      </body>
    </html>
  );
}
```

---

### 第二阶段:静态内容翻译 (1小时)

#### Step 4: 创建翻译字典

**文件**: `src/i18n/dictionaries/zh.ts`

```typescript
export default {
  // ========== 导航栏 ==========
  nav: {
    home: '首页',
    about: '关于',
    reports: '报告',
    contact: '联系',
  },

  // ========== 首页 ==========
  home: {
    hero: {
      title: 'YECO Studio',
      subtitle: '创意设计与洞察研究',
      cta: '探索报告',
    },
    featured: {
      title: '精选报告',
      viewAll: '查看全部',
    },
  },

  // ========== 关于页 ==========
  about: {
    title: '关于我们',
    description: 'YECO Studio 是一家专注于...',
  },

  // ========== 报告列表页 ==========
  reports: {
    title: '研究报告',
    categories: {
      all: '全部',
      consumerInsight: '消费者洞察',
      designTrend: '设计趋势',
      marketAnalysis: '市场分析',
    },
    readMore: '阅读更多',
  },

  // ========== 联系表单 ==========
  contact: {
    title: '联系我们',
    name: '姓名',
    email: '邮箱',
    message: '留言',
    submit: '发送',
    sending: '发送中...',
    success: '发送成功',
    error: '发送失败,请重试',
  },

  // ========== 通用 ==========
  common: {
    loading: '加载中...',
    error: '出错了',
    back: '返回',
    next: '下一页',
    prev: '上一页',
    publishedOn: '发布于',
    readTime: '分钟阅读',
  },
};
```

**文件**: `src/i18n/dictionaries/en.ts`

```typescript
export default {
  nav: {
    home: 'Home',
    about: 'About',
    reports: 'Reports',
    contact: 'Contact',
  },

  home: {
    hero: {
      title: 'YECO Studio',
      subtitle: 'Creative Design & Insight Research',
      cta: 'Explore Reports',
    },
    featured: {
      title: 'Featured Reports',
      viewAll: 'View All',
    },
  },

  about: {
    title: 'About Us',
    description: 'YECO Studio is a creative agency focused on...',
  },

  reports: {
    title: 'Research Reports',
    categories: {
      all: 'All',
      consumerInsight: 'Consumer Insight',
      designTrend: 'Design Trend',
      marketAnalysis: 'Market Analysis',
    },
    readMore: 'Read More',
  },

  contact: {
    title: 'Contact Us',
    name: 'Name',
    email: 'Email',
    message: 'Message',
    submit: 'Send',
    sending: 'Sending...',
    success: 'Sent successfully',
    error: 'Failed to send, please retry',
  },

  common: {
    loading: 'Loading...',
    error: 'Something went wrong',
    back: 'Back',
    next: 'Next',
    prev: 'Previous',
    publishedOn: 'Published on',
    readTime: 'min read',
  },
};
```

**文件**: `src/i18n/dictionaries/index.ts`

```typescript
import zh from './zh';
import en from './en';

export const dictionaries = { zh, en };
export type Dictionary = typeof zh;
```

#### Step 5: 创建翻译钩子

**文件**: `src/i18n/hooks/useTranslation.ts`

```typescript
import { useLanguage } from '../LanguageContext';
import { dictionaries, Dictionary } from '../dictionaries';

export function useTranslation() {
  const { language } = useLanguage();
  const t = dictionaries[language];

  return { t, language };
}
```

---

### 第三阶段:动态内容翻译 (1.5小时)

#### Step 6: 扩展报告数据结构

**文件**: `src/app/reports/_data/reports-data.ts`

修改数据类型,支持多语言:

```typescript
export interface IReportItem {
  id: number;
  slug: string;

  // 多语言标题
  title: {
    zh: string;
    en: string;
  };

  // 多语言副标题(可选)
  subtitle?: {
    zh: string;
    en: string;
  };

  // 分类保持单一,通过翻译字典转换显示
  category: ReportCategory;

  coverImage: string;
  bannerImage: string;
  year: string;
  month: string;
  date: string;

  // 多语言描述
  description: {
    zh: string;
    en: string;
  };
}

// 示例数据(更新格式)
const reports_data: IReportItem[] = [
  {
    id: 1,
    slug: "2025-10-middleeast",
    title: {
      zh: "中东香水市场洞察",
      en: "Middle East Perfume Market Insights"
    },
    subtitle: {
      zh: "沙特阿联酋市场深度分析",
      en: "In-depth Analysis of Saudi & UAE Markets"
    },
    category: "市场分析",
    coverImage: "/assets/...",
    bannerImage: "/assets/...",
    year: "2025",
    month: "10",
    date: "2025-10-15",
    description: {
      zh: "深入分析中东地区香水消费习惯、市场规模及增长趋势,揭示阿拉伯香水文化的独特魅力。",
      en: "In-depth analysis of perfume consumption habits, market size and growth trends in the Middle East, revealing the unique charm of Arab perfume culture."
    },
  },
  // ... 其他报告
];
```

#### Step 7: 创建报告数据翻译助手

**文件**: `src/app/reports/_data/report-helpers.ts`

```typescript
import { Language } from '@/i18n/config';
import { IReportItem } from './reports-data';

// 获取翻译后的报告标题
export function getReportTitle(report: IReportItem, lang: Language): string {
  return report.title[lang] || report.title.zh;
}

// 获取翻译后的报告副标题
export function getReportSubtitle(report: IReportItem, lang: Language): string | undefined {
  return report.subtitle?.[lang] || report.subtitle?.zh;
}

// 获取翻译后的报告描述
export function getReportDescription(report: IReportItem, lang: Language): string {
  return report.description[lang] || report.description.zh;
}

// 分类名称翻译(连接到字典)
export function getCategoryName(category: string, lang: Language): string {
  const categoryMap: Record<string, Record<Language, string>> = {
    '消费者洞察': { zh: '消费者洞察', en: 'Consumer Insight' },
    '设计趋势': { zh: '设计趋势', en: 'Design Trend' },
    '市场分析': { zh: '市场分析', en: 'Market Analysis' },
  };
  return categoryMap[category]?.[lang] || category;
}
```

---

### 第四阶段:组件适配 (1小时)

#### Step 8: 报告列表组件适配

**文件**: `src/components/reports/reports-grid-area.tsx`

```typescript
'use client';

import { useTranslation } from '@/i18n/hooks/useTranslation';
import { getReportTitle, getReportDescription, getCategoryName } from '@/app/reports/_data/report-helpers';
import reports_data from '@/app/reports/_data/reports-data';

export default function ReportsGridArea() {
  const { t, language } = useTranslation();

  return (
    <section>
      <h1>{t.reports.title}</h1>

      {/* 分类筛选 */}
      <div className="filter-buttons">
        <button data-filter="*">{t.reports.categories.all}</button>
        <button data-filter=".cat1">{t.reports.categories.consumerInsight}</button>
        <button data-filter=".cat2">{t.reports.categories.designTrend}</button>
        <button data-filter=".cat3">{t.reports.categories.marketAnalysis}</button>
      </div>

      {/* 报告网格 */}
      <div className="reports-grid">
        {reports_data.map((report) => (
          <div key={report.id} className={`report-item ${getCategoryClass(report.category)}`}>
            <h3>{getReportTitle(report, language)}</h3>
            <p>{getReportDescription(report, language)}</p>
            <span className="category">{getCategoryName(report.category, language)}</span>
          </div>
        ))}
      </div>
    </section>
  );
}
```

#### Step 9: 报告详情页适配

**文件**: `src/app/reports/[slug]/page.tsx`

```typescript
import { getReportBySlug } from '@/app/reports/_data/reports-data';
import { getReportTitle, getReportDescription } from '@/app/reports/_data/report-helpers';
import ReportContent from '../_content/[slug]/index'; // 动态导入对应报告组件

export async function generateMetadata({ params }: { params: { slug: string } }) {
  const report = getReportBySlug(params.slug);
  if (!report) return { title: 'Report Not Found' };

  // 这里可以根据用户语言偏好动态设置 metadata
  // 暂时使用中文作为默认 SEO
  return {
    title: report.title.zh,
    description: report.description.zh,
  };
}

export default function ReportDetailPage({ params }: { params: { slug: string } }) {
  const report = getReportBySlug(params.slug);

  if (!report) {
    return <div>报告未找到 / Report Not Found</div>;
  }

  return (
    <div>
      {/* 报告组件本身会处理多语言内容 */}
      <ReportContent />
    </div>
  );
}
```

---

### 第五阶段:报告正文多语言 (在AI生成时集成)

#### Step 10: 更新报告生成SOP

在 `新报告生成SOP规范.md` 中添加多语言要求:

**新增章节**: 多语言内容创作规范

```markdown
## 第六步: 创建多语言报告组件

### 6.1 组件结构要求

每个报告组件必须支持中英文切换:

- 使用 `useTranslation()` 钩子获取当前语言
- 根据 language 值渲染对应语言的内容
- 保持 HTML 结构一致,仅文本内容切换

### 6.2 实现示例

\`\`\`typescript
'use client';

import { useTranslation } from '@/i18n/hooks/useTranslation';
import styles from './style.module.css';

export default function Report2025MiddleEast() {
  const { language } = useTranslation();

  return (
    <div className={styles.reportWrapper}>
      {language === 'zh' ? (
        <>
          <h1>中东香水市场洞察</h1>
          <p>沙特阿拉伯和阿联酋是全球香水消费最高的市场...</p>
        </>
      ) : (
        <>
          <h1>Middle East Perfume Market Insights</h1>
          <p>Saudi Arabia and UAE are among the highest perfume-consuming markets globally...</p>
        </>
      )}
    </div>
  );
}
\`\`\`

### 6.3 内容创作要求

- **优先级**: 先完成中文版本,再创作英文版本
- **翻译质量**: 英文版本应为专业的营销文案,而非直译
- **术语一致性**: 使用行业标准术语
- **数据统一**: 数字、日期、图表在两个语言版本中保持一致
```

---

### 第六阶段:语言切换器 (30分钟)

#### Step 11: 创建极简语言切换组件

**文件**: `src/components/common/LanguageSwitcher.tsx`

```typescript
'use client';

import { useLanguage } from '@/i18n/LanguageContext';
import { languages, languageNames } from '@/i18n/config';
import styles from './LanguageSwitcher.module.css';

export default function LanguageSwitcher() {
  const { language, setLanguage } = useLanguage();

  return (
    <div className={styles.switcher}>
      {languages.map((lang) => (
        <button
          key={lang}
          onClick={() => setLanguage(lang)}
          className={language === lang ? styles.active : ''}
          aria-label={`Switch to ${languageNames[lang]}`}
        >
          {languageNames[lang]}
        </button>
      ))}
    </div>
  );
}
```

**文件**: `src/components/common/LanguageSwitcher.module.css`

```css
/* 符合 YECO 极简设计理念的语言切换器 */
.switcher {
  display: flex;
  gap: 0.5rem;
  font-family: var(--tp-ff-body);
  font-size: 0.875rem;
}

.switcher button {
  padding: 0.25rem 0.5rem;
  background: none;
  border: none;
  color: rgba(255, 255, 255, 0.5);
  cursor: pointer;
  transition: color 0.2s ease;
  font-weight: 400;
}

.switcher button:hover {
  color: rgba(255, 255, 255, 0.8);
}

.switcher button.active {
  color: #ffffff;
  border-bottom: 1px solid #ffffff;
}
```

#### Step 12: 集成到导航栏

在主导航组件中引入 LanguageSwitcher:

```typescript
import LanguageSwitcher from '@/components/common/LanguageSwitcher';

export default function Navigation() {
  return (
    <nav>
      {/* 原有导航项 */}
      <LanguageSwitcher />
    </nav>
  );
}
```

---

## 🔄 工作流程整合

### 新报告发布流程(含多语言)

1. **创建中文内容**
   - 在 `reports-data.ts` 添加报告元数据(中文字段)
   - 创建报告组件,完成中文版内容

2. **添加英文翻译**
   - 在 `reports-data.ts` 添加英文标题、副标题、描述
   - 在报告组件中添加英文版内容(使用条件渲染)

3. **测试多语言切换**
   - 检查语言切换器是否正常工作
   - 验证两种语言的内容显示是否完整
   - 确认 CSS 样式在不同语言下的兼容性

4. **提交发布**
   - Git commit 包含中英文内容
   - 部署到生产环境

### 静态内容翻译流程

1. **识别新增静态文本**
   - 在组件中发现硬编码的中文文本

2. **添加到字典**
   - 在 `src/i18n/dictionaries/zh.ts` 添加新键值
   - 在 `src/i18n/dictionaries/en.ts` 添加对应英文翻译

3. **组件改造**
   - 使用 `useTranslation()` 钩子
   - 将硬编码文本替换为 `t.xxx.xxx`

---

## 📊 翻译优先级策略

### 高优先级(立即翻译)
- 导航栏
- 首页 Hero 区域
- 报告元数据(标题、描述)
- 联系表单
- 错误提示

### 中优先级(2周内翻译)
- 关于页核心内容
- 报告详情页框架文本
- Footer 内容

### 低优先级(按需翻译)
- 旧报告的正文内容(仅翻译热门报告)
- 深度文章段落
- 图片内的文字(可保持中文)

---

## 🎯 SEO 优化建议

### 当前方案的 SEO 特点

**优点**:
- 无 URL 分离,对中文市场友好
- 实现简单,维护成本低

**局限**:
- 英文内容 SEO 权重较低
- 无法为不同语言设置独立 metadata

### 未来升级方向(可选)

如果需要更好的国际化 SEO,可以考虑:

1. **URL 结构升级**
   - `/reports/[slug]` → `/zh/reports/[slug]` 和 `/en/reports/[slug]`
   - 需要配置 Next.js i18n routing

2. **动态 Metadata**
   - 根据用户语言偏好生成不同的 `<title>` 和 `<meta description>`

3. **Hreflang 标签**
   - 添加 `<link rel="alternate" hreflang="en" />` 等标签
   - 告知搜索引擎多语言版本的关系

---

## ✅ 完成检查清单

### 开发阶段
- [ ] 创建 i18n 文件夹结构
- [ ] 实现 LanguageContext 和 LanguageProvider
- [ ] 创建中英文字典文件
- [ ] 扩展 reports-data.ts 支持多语言
- [ ] 创建翻译助手函数
- [ ] 适配现有组件(导航、报告列表、报告详情)
- [ ] 实现语言切换器组件
- [ ] 更新报告生成 SOP

### 测试阶段
- [ ] 语言切换器正常工作
- [ ] 所有静态文本正确翻译
- [ ] 报告元数据显示正确
- [ ] localStorage 语言偏好持久化
- [ ] 页面刷新后语言不重置
- [ ] 中英文排版无错位

### 内容阶段
- [ ] 翻译现有30篇报告的元数据
- [ ] 翻译3-5篇重点报告的正文
- [ ] 检查专业术语一致性
- [ ] 校对英文文案质量

---

## 🚀 扩展能力

此方案具备良好的扩展性,未来可以:

1. **添加更多语言**
   - 在 `config.ts` 添加新语言代码
   - 创建对应的字典文件 `src/i18n/dictionaries/xx.ts`
   - 无需修改组件代码

2. **引入翻译管理平台**
   - 可对接 Phrase, Lokalise 等平台
   - 字典文件可由 CMS 自动生成

3. **AI 辅助翻译**
   - 使用 GPT API 自动生成初稿
   - 人工审校后更新字典

---

## 💡 设计理念对齐

此多语言方案完全遵循 YECO 的极简主义设计哲学:

**克制**:
- 无花哨的语言切换动画
- 纯文字切换器,无国旗图标
- 最小化的视觉干扰

**优雅**:
- 统一的字体系统(Noto Serif SC 支持中文,Syne 支持英文)
- 保持黑白灰主色调
- 语言切换无闪烁

**永恒**:
- 不依赖外部库,避免技术债务
- 简单的文件结构,易于长期维护
- 标准化的翻译流程,方便团队协作

---

**总结**: 此方案用最轻量的方式实现了全站统一的多语言管理,特别照顾到静态内容和动态内容的不同特点,既保证了灵活性,又维持了 YECO 品牌的一致性。

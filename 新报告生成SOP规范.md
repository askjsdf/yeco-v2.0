# 新报告生成 SOP 规范

## 📋 使用说明

本文档是为 AI 助手编写的标准操作流程(SOP)。当你收到一份 PDF 报告文档和本 SOP 时,请严格按照以下步骤操作,完成后用户刷新页面即可在报告列表中看到新报告,点击可正常访问详情页。

---

## 🏗️ 项目架构概览

在开始操作前,请先理解本项目的整体架构设计理念。

### 核心设计理念

本报告系统采用 **模块化 + 自动化** 的架构思想:

1. **模块化独立**: 每篇报告是一个独立的 React 组件,拥有自己的样式和内容
2. **自动化集成**: 只需创建必要文件,系统自动完成路由和页面组装
3. **内容自由**: 报告内部的设计、布局、交互完全自由发挥
4. **规范接入**: 通过固定的文件结构和数据格式融入系统

### 系统工作原理

```
用户访问流程:
/reportsList (列表页)
    ↓ 点击报告卡片
/reports/[slug] (详情页)
    ↓ 系统自动加载
Header + 报告组件 + Footer
```

系统会根据 URL 中的 `slug` 自动:
- 从数据中心读取报告元信息
- 动态加载对应的报告组件
- 组装 Header、Footer 和报告内容
- 生成"相关报告"推荐

**你只需要**: 提供数据 + 创建组件,其余全部自动化!

### 完整文件结构

```
项目根目录/
│
├── src/
│   ├── app/
│   │   ├── (homes)/
│   │   │   └── reportsList/           # 📄 报告列表页路由
│   │   │       └── page.tsx           # 列表页入口
│   │   │
│   │   └── reports/                   # 📁 报告系统核心目录
│   │       ├── [slug]/                # 🔀 动态路由 (自动处理)
│   │       │   └── page.tsx           # 详情页入口 (无需修改)
│   │       │
│   │       ├── _components/           # 📦 报告通用组件
│   │       │   └── report-detail.tsx  # 详情页容器 (无需修改)
│   │       │
│   │       ├── _content/              # 📝 报告内容模块 (⚠️ 你的工作区)
│   │       │   ├── 2025-10-middleeast/
│   │       │   │   ├── index.tsx      # 报告正文组件
│   │       │   │   └── style.module.css
│   │       │   │
│   │       │   └── [新报告slug]/      # ⭐ 你需要在这里创建新文件夹
│   │       │       ├── index.tsx      # ⭐ 创建报告组件
│   │       │       └── style.module.css # ⭐ 创建样式文件(可选)
│   │       │
│   │       └── _data/                 # 📊 数据中心
│   │           └── reports-data.ts    # ⭐ 在这里注册报告数据
│   │
│   ├── components/reports/            # 🎨 报告展示组件
│   │   ├── reports-banner-slider.tsx  # 轮播组件 (无需修改)
│   │   └── reports-grid-area.tsx      # 网格组件 (无需修改)
│   │
│   ├── layouts/
│   │   ├── headers/                   # 🎯 Header (系统自动加载)
│   │   └── footers/                   # 🎯 Footer (系统自动加载)
│   │
│   └── pages/reports/
│       └── reports-list.tsx           # 列表页主组件 (无需修改)
│
└── public/
    └── assets/
        └── reports/                   # 🖼️ 报告图片资源
            ├── 2025-10-middleeast/
            │   ├── 01主视觉.png
            │   ├── 02核心洞察配图1网图.png
            │   └── ...
            │
            └── [新报告slug]/          # ⭐ 你需要在这里创建图片文件夹
                ├── cover.jpg          # ⭐ 列表页封面图
                ├── banner.jpg         # ⭐ 轮播背景图
                └── 01-xxx.png         # ⭐ 报告内图片
```

### 关键文件说明

| 文件路径 | 作用 | 是否需要修改 |
|---------|------|-------------|
| `src/app/reports/_data/reports-data.ts` | 报告数据注册中心 | ⭐ **需要** - 添加新报告数据 |
| `src/app/reports/_content/[slug]/index.tsx` | 报告内容组件 | ⭐ **需要** - 创建新报告组件 |
| `public/assets/reports/[slug]/` | 报告图片资源 | ⭐ **需要** - 创建图片文件夹 |
| `src/app/reports/[slug]/page.tsx` | 动态路由页面 | ❌ **无需修改** - 自动处理 |
| `src/app/reports/_components/report-detail.tsx` | 详情页容器 | ❌ **无需修改** - 自动组装 |
| `src/layouts/headers/`, `src/layouts/footers/` | Header/Footer | ❌ **无需修改** - 自动加载 |

### 数据流转示意

```
1. 用户访问 /reportsList
   ↓
2. 从 reports-data.ts 读取所有报告数据
   ↓
3. 渲染报告列表 + 轮播

4. 用户点击报告卡片 (例: slug = "2025-12-china-market")
   ↓
5. 跳转到 /reports/2025-12-china-market
   ↓
6. 系统自动:
   - 从 reports-data.ts 获取报告元信息
   - 动态加载 _content/2025-12-china-market/index.tsx
   - 组装: Header + 报告组件 + 相关推荐 + Footer
   ↓
7. 完整页面呈现给用户
```

### 自动化机制解析

#### 路由自动化
```typescript
// src/app/reports/[slug]/page.tsx (无需修改)
// 系统会自动根据 URL 中的 [slug] 动态加载对应组件

export default function ReportDetailPage() {
  const params = useParams();
  const slug = params?.slug;  // 从 URL 获取 slug

  // 自动动态导入组件
  const DynamicComponent = dynamic(() => import(`../_content/${slug}`));

  return <DynamicComponent />;
}
```

#### 数据自动化
```typescript
// 从数据中心自动读取
const report = getReportBySlug(slug);  // 获取报告元信息
const relatedReports = getReportsByCategory(report.category);  // 自动推荐
```

### 你的工作范围

**✅ 你需要做的** (3个步骤):
1. 在 `reports-data.ts` 添加一条数据记录
2. 在 `_content/[slug]/` 创建报告组件
3. 在 `public/assets/reports/[slug]/` 创建图片文件夹

**❌ 你不需要做的** (系统自动):
- 创建路由文件
- 引入 Header/Footer
- 编写列表页展示逻辑
- 处理相关报告推荐
- 配置动态导入

### 核心约束

为了让系统自动化工作,你必须遵守以下约束:

1. **slug 一致性**: 在数据、文件夹、图片路径三处使用完全相同的 slug
2. **文件命名**: 组件文件必须命名为 `index.tsx`
3. **导出方式**: 组件必须使用 `export default`
4. **客户端组件**: 组件开头必须添加 `'use client';`
5. **图片组件**: 必须使用 Next.js 的 `Image` 组件

### 设计自由度

在遵守上述约束的前提下,你对报告内容拥有**完全的自由**:

- ✅ 任意 HTML 结构和布局设计
- ✅ 自定义 CSS 样式和动画效果
- ✅ 使用 React hooks 实现交互
- ✅ 引入第三方库(图表、动画等)
- ✅ 自由的内容组织方式
- ✅ 独特的视觉设计风格

**核心理念**: 系统提供"管道"(数据流转、路由、页面组装),你提供"内容"(报告组件)。

---

## 🎯 操作目标

将 PDF 报告内容转化为网页版报告,并自动集成到现有系统中。

**你将收到**: 一份 PDF 报告文档
**你需要完成**: 按顺序执行以下 3 个步骤
**系统自动处理**: Header、Footer、路由加载、相关推荐

---

## 📝 Step 1: 注册报告数据

### 操作文件
```
src/app/reports/_data/reports-data.ts
```

### 操作内容

在 `reports_data` 数组的**末尾**添加新的报告对象:

```typescript
{
  id: [下一个递增的数字],
  slug: "年份-月份-关键词",  // ⚠️ 重要: 后续步骤都使用这个 slug
  title: "报告标题",
  subtitle: "报告副标题",     // 可选,没有则填空字符串 ""
  category: "分类名称",       // 必须从以下三个选择: "消费者洞察" | "设计趋势" | "市场分析"
  coverImage: "/assets/reports/[slug]/封面图文件名.jpg",
  bannerImage: "/assets/reports/[slug]/横幅图文件名.jpg",
  year: "2025",
  month: "12",
  date: "2025-12-09",        // 格式: YYYY-MM-DD
  description: "报告的一句话简介,用于列表页展示",
}
```

### 字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| `id` | 递增数字,比当前最大 id 大 1 | `31` |
| `slug` | URL 标识,格式为"年份-月份-关键词",**全小写英文和数字,用短横线连接** | `"2025-12-china-market"` |
| `title` | 报告标题,中文 | `"中国市场深度分析"` |
| `subtitle` | 副标题(可选) | `"2025年第四季度"` |
| `category` | 必须是三个固定值之一 | `"市场分析"` |
| `coverImage` | 封面图路径,用于列表页卡片 | `"/assets/reports/2025-12-china-market/cover.jpg"` |
| `bannerImage` | 横幅大图路径,用于轮播背景 | `"/assets/reports/2025-12-china-market/banner.jpg"` |
| `year` | 年份 | `"2025"` |
| `month` | 月份 | `"12"` |
| `date` | 完整日期 | `"2025-12-09"` |
| `description` | 简短描述,1-2句话 | `"深入分析2025年中国香水市场趋势与消费者行为变化"` |

### ⚠️ 关键注意事项

1. **slug 命名规则**:
   - 全部小写
   - 只使用英文字母、数字、短横线 `-`
   - 推荐格式: `年份-月份-关键词` (例: `2025-12-china-market`)
   - **slug 在后续步骤中必须保持完全一致**

2. **图片路径规范**:
   - 必须以 `/assets/reports/[slug]/` 开头
   - 建议文件名使用英文 (例: `cover.jpg`, `banner.jpg`)

3. **category 只能从以下三个选择**:
   - `"消费者洞察"`
   - `"设计趋势"`
   - `"市场分析"`

### 完成标志
- ✅ 数据对象已添加到数组末尾
- ✅ 所有必填字段已填写
- ✅ slug 符合命名规则
- ✅ 保存文件无语法错误

---

## 🎨 Step 2: 创建报告组件

### 操作步骤

#### 2.1 创建文件夹
```
src/app/reports/_content/[slug]/
```
⚠️ **[slug] 必须与 Step 1 中的 slug 完全一致**

#### 2.2 创建组件文件

在上述文件夹中创建 `index.tsx`:

```typescript
'use client';

import { useEffect, useRef } from 'react';
import Image from 'next/image';
import styles from './style.module.css';

// 图片基础路径
const IMG_BASE = '/assets/reports/[slug]';  // ⚠️ 替换为实际的 slug

const Report报告名称 = () => {
  return (
    <div className={styles.reportWrapper}>
      {/*
        🎨 在这里根据 PDF 内容自由编写报告结构

        你可以:
        - 使用任意 HTML 结构和布局
        - 创建自定义样式和动画
        - 使用 React hooks 实现交互效果
        - 引入第三方图表库(如需要)

        约束:
        - 不要引入 Header、Footer 组件(系统自动加载)
        - 图片必须使用 Next.js Image 组件
        - 图片路径使用 IMG_BASE 常量
      */}

      {/* 示例: 封面区域 */}
      <section className={styles.coverPage}>
        <h1>报告标题</h1>
        <p>根据 PDF 内容自由发挥</p>
      </section>

      {/* 示例: 图片使用 */}
      <div className={styles.imageContainer}>
        <Image
          src={`${IMG_BASE}/01-示例图片.png`}  // ⚠️ 需要准备: 示例图片,建议尺寸 1200x800
          alt="图片描述"
          fill
          style={{ objectFit: 'cover' }}
        />
      </div>

      {/* 继续添加报告内容... */}
    </div>
  );
};

export default Report报告名称;  // ⚠️ 必须使用 export default
```

#### 2.3 创建样式文件(可选)

在同一文件夹创建 `style.module.css`:

```css
/* ==========================================
   报告样式表
   ========================================== */

.reportWrapper {
    /* 基础样式 */
    font-family: var(--tp-ff-noto-serif-sc), serif;
    background: #000;
    color: #fff;
    line-height: 1.8;

    margin: 0;
    padding: 0;
    width: 100%;
    overflow-x: hidden;
}

.coverPage {
    padding: 120px 40px;
    text-align: center;
}

.imageContainer {
    position: relative;
    width: 100%;
    height: 400px;
    background: #f0f0f0;  /* 占位符背景 */
}

/* 添加更多自定义样式... */

/* 移动端适配 */
@media (max-width: 768px) {
    .coverPage {
        padding: 60px 20px;
    }
}
```

### ⚠️ 关键技术要点

#### 必须遵守的约束

1. **组件开头必须添加**: `'use client';`
2. **必须默认导出组件**: `export default ComponentName;`
3. **不要引入 Header/Footer**: 系统会自动加载
4. **图片必须使用 Next.js Image 组件**:
   ```typescript
   import Image from 'next/image';
   ```

#### 图片使用规范

```typescript
// 1. 定义图片基础路径
const IMG_BASE = '/assets/reports/你的slug';

// 2. 使用占位符容器和清晰的注释
<div style={{
  position: 'relative',
  width: '100%',
  height: '400px',
  background: '#f0f0f0'  // 灰色占位背景
}}>
  <Image
    src={`${IMG_BASE}/01-封面主视觉.png`}  // ⚠️ 需要准备: 封面主视觉图,建议尺寸 1200x800
    alt="封面主视觉"
    fill
    style={{ objectFit: 'cover' }}
  />
</div>
```

#### 样式文件使用

- 推荐使用 **CSS Modules** (文件名: `style.module.css`)
- 样式会自动隔离,不会影响其他页面
- 可以使用全局 CSS 变量: `var(--tp-ff-noto-serif-sc)`

### 内容开发自由度

你可以**完全自由地**设计报告内容:

✅ **可以做的**:
- 任意 HTML 结构和布局
- 自定义 CSS 样式和动画
- 使用 React hooks (useState, useEffect, useRef 等)
- 引入第三方库(图表库、动画库等)
- 实现滚动动画、交互效果
- 使用自己的设计风格

❌ **不要做的**:
- 不要引入 Header 或 Footer 组件
- 不要修改路由文件
- 不要使用绝对定位覆盖整个页面

### 完成标志
- ✅ 文件夹名称与 slug 一致
- ✅ `index.tsx` 包含 `'use client'` 和 `export default`
- ✅ 图片路径使用了占位符和注释说明
- ✅ 组件可以独立渲染(没有语法错误)

---

## 🖼️ Step 3: 创建图片文件夹并使用占位符

### 操作步骤

#### 3.1 创建图片文件夹
```
public/assets/reports/[slug]/
```
⚠️ **[slug] 必须与 Step 1 中的 slug 完全一致**

#### 3.2 在代码中使用占位符

在 Step 2 的组件中,所有需要图片的地方使用以下模式:

```typescript
// 定义图片基础路径
const IMG_BASE = '/assets/reports/你的slug';

// 使用占位符容器
<div style={{
  position: 'relative',
  width: '100%',
  height: '500px',  // 根据实际需求调整
  background: '#f0f0f0'  // 灰色占位背景,图片加载前可见
}}>
  <Image
    src={`${IMG_BASE}/描述性文件名.png`}  // ⚠️ 需要准备: [图片用途说明], 建议尺寸 [宽x高]
    alt="图片描述"
    fill
    style={{ objectFit: 'cover' }}
  />
</div>
```

### 图片命名建议

使用**描述性的文件名**,方便用户理解需要准备什么图片:

✅ **推荐命名**:
- `01-封面主视觉.png`
- `02-数据图表-市场规模.png`
- `03-案例分析-品牌对比.jpg`
- `04-趋势分析配图.png`

❌ **避免命名**:
- `image1.png`
- `pic.jpg`
- `photo.png`

### 注释说明规范

每个图片都应该有清晰的注释,说明:
1. 图片用途
2. 建议尺寸
3. 内容要求(可选)

```typescript
<Image
  src={`${IMG_BASE}/01-封面主视觉.png`}
  // ⚠️ 需要准备:
  // - 用途: 封面主视觉背景图
  // - 建议尺寸: 1920x1080
  // - 内容: 中东风格的香水瓶或市场场景
  alt="封面主视觉"
  fill
  style={{ objectFit: 'cover' }}
/>
```

### 常用图片尺寸参考

| 用途 | 建议尺寸 | 说明 |
|------|----------|------|
| 封面横幅 | 1920x1080 | 全屏背景图 |
| 内容配图 | 1200x800 | 标准横图 |
| 竖图配图 | 800x1200 | 竖版图片 |
| 图表截图 | 1000x600 | 数据可视化 |
| 产品图 | 800x800 | 正方形产品展示 |

### 完成标志
- ✅ 文件夹 `public/assets/reports/[slug]/` 已创建
- ✅ 代码中所有图片位置已用占位符标记
- ✅ 每个图片都有清晰的文件名和注释说明
- ✅ 占位符使用了灰色背景 (`background: '#f0f0f0'`)

### 📌 后续操作 (由用户完成)

用户会根据你代码中的注释:
1. 准备对应的图片文件
2. 将图片放入 `public/assets/reports/[slug]/` 文件夹
3. 确保文件名与代码中的路径完全一致
4. 刷新页面,图片自动显示

---

## ✅ 验证清单

完成所有步骤后,请逐项检查:

### 数据检查
- [ ] `reports-data.ts` 中添加了新的报告对象
- [ ] `id` 是递增的数字
- [ ] `slug` 符合命名规则(全小写,只有字母数字和短横线)
- [ ] `category` 是三个固定值之一
- [ ] `coverImage` 和 `bannerImage` 路径正确
- [ ] 所有必填字段已填写

### 文件结构检查
- [ ] 文件夹 `src/app/reports/_content/[slug]/` 已创建
- [ ] 文件夹名称与数据中的 `slug` 完全一致
- [ ] `index.tsx` 文件已创建
- [ ] 文件开头有 `'use client';` 指令
- [ ] 组件使用了 `export default`
- [ ] 如果有样式文件,已创建 `style.module.css`

### 图片资源检查
- [ ] 文件夹 `public/assets/reports/[slug]/` 已创建
- [ ] 文件夹名称与 `slug` 一致
- [ ] 代码中定义了 `IMG_BASE` 常量
- [ ] 所有图片使用了占位符容器(灰色背景)
- [ ] 每个图片都有清晰的注释说明

### 代码质量检查
- [ ] 组件没有引入 Header 或 Footer
- [ ] 图片使用了 Next.js Image 组件
- [ ] 没有语法错误
- [ ] CSS 类名使用了模块化(styles.xxx)

---

## 🧪 测试步骤

开发完成后,告知用户进行以下测试:

1. **列表页测试**:
   - 访问 `/reportsList`
   - 检查新报告是否显示在网格中
   - 检查封面图占位符是否正常显示

2. **详情页测试**:
   - 访问 `/reports/[你的slug]`
   - 检查报告内容是否正常显示
   - 检查图片占位符(灰色背景)是否正常

3. **路由测试**:
   - 从列表页点击报告卡片
   - 应该能正常跳转到详情页
   - 检查页面底部的"相关报告"推荐是否显示

4. **响应式测试**:
   - 在不同屏幕尺寸下查看页面
   - 移动端布局是否正常

---

## 🔧 常见问题排查

### 问题: 报告没有显示在列表页

**可能原因**:
- `reports-data.ts` 数据格式错误
- `category` 拼写错误
- 文件保存后没有自动重新编译

**解决方案**:
- 检查数据对象的语法(逗号、引号)
- 确认 `category` 是三个固定值之一
- 重启开发服务器

### 问题: 访问详情页出现 404

**可能原因**:
- 组件文件夹名称与 `slug` 不一致
- `index.tsx` 文件名拼写错误
- 组件没有默认导出

**解决方案**:
- 检查文件夹名称与 `slug` 是否完全一致
- 确认文件名是 `index.tsx` (小写)
- 确认组件使用了 `export default`

### 问题: 图片不显示

**可能原因**:
- 图片文件夹路径错误
- 图片文件名与代码中的路径不一致
- 图片还未准备(这是正常的,应该看到灰色占位符)

**解决方案**:
- 检查 `public/assets/reports/[slug]/` 路径是否正确
- 图片准备好后,确保文件名与代码完全一致
- 开发阶段看到灰色占位符是正常的

### 问题: 样式不生效

**可能原因**:
- CSS 文件名不正确
- import 语句错误
- 类名使用错误

**解决方案**:
- 确认文件名是 `style.module.css`
- 检查 import 语句: `import styles from './style.module.css';`
- 使用类名时: `className={styles.yourClassName}`

---

## 📚 完整示例

### 示例场景
创建一篇 "2025年12月中国市场报告"

---

### Step 1: 在 `reports-data.ts` 添加数据

```typescript
{
  id: 31,
  slug: "2025-12-china-market",
  title: "中国市场深度分析",
  subtitle: "2025年第四季度消费趋势",
  category: "市场分析",
  coverImage: "/assets/reports/2025-12-china-market/cover.jpg",
  bannerImage: "/assets/reports/2025-12-china-market/banner.jpg",
  year: "2025",
  month: "12",
  date: "2025-12-09",
  description: "深入分析2025年中国香水市场规模、增长趋势及消费者行为变化",
}
```

---

### Step 2: 创建组件

**文件**: `src/app/reports/_content/2025-12-china-market/index.tsx`

```typescript
'use client';

import { useEffect, useRef } from 'react';
import Image from 'next/image';
import styles from './style.module.css';

// 图片基础路径
const IMG_BASE = '/assets/reports/2025-12-china-market';

const Report2025ChinaMarket = () => {
  return (
    <div className={styles.reportWrapper}>
      {/* 封面 */}
      <section className={styles.coverPage}>
        <div className={styles.coverContent}>
          <span className={styles.coverCategory}>市场分析</span>
          <h1 className={styles.coverTitle}>中国市场深度分析</h1>
          <p className={styles.coverSubtitle}>2025年第四季度消费趋势</p>
          <div className={styles.coverMeta}>
            <span>2025.12</span>
            <span>YECO Studio</span>
          </div>
        </div>

        {/* 封面背景图 */}
        <div className={styles.coverBackground}>
          <Image
            src={`${IMG_BASE}/01-封面主视觉.jpg`}
            // ⚠️ 需要准备:
            // - 用途: 封面背景图
            // - 建议尺寸: 1920x1080
            // - 内容: 中国市场相关场景或产品图
            alt="封面背景"
            fill
            style={{ objectFit: 'cover', opacity: 0.3 }}
          />
        </div>
      </section>

      {/* 执行摘要 */}
      <section className={styles.summarySection}>
        <div className={styles.container}>
          <h2 className={styles.sectionTitle}>执行摘要</h2>
          <p className={styles.summaryText}>
            根据 PDF 内容填写摘要文字...
          </p>

          {/* 核心数据配图 */}
          <div className={styles.imageBox}>
            <Image
              src={`${IMG_BASE}/02-市场规模数据图.png`}
              // ⚠️ 需要准备:
              // - 用途: 市场规模数据可视化
              // - 建议尺寸: 1200x800
              // - 内容: 柱状图或饼图展示市场数据
              alt="市场规模数据"
              fill
              style={{ objectFit: 'contain' }}
            />
          </div>
        </div>
      </section>

      {/* 继续添加其他章节... */}
      <section className={styles.analysisSection}>
        <div className={styles.container}>
          <h2 className={styles.sectionTitle}>趋势分析</h2>
          {/* 根据 PDF 内容自由发挥 */}
        </div>
      </section>
    </div>
  );
};

export default Report2025ChinaMarket;
```

---

### Step 2.5: 创建样式文件

**文件**: `src/app/reports/_content/2025-12-china-market/style.module.css`

```css
.reportWrapper {
    font-family: var(--tp-ff-noto-serif-sc), serif;
    background: #000;
    color: #fff;
    margin: 0;
    padding: 0;
    width: 100%;
    overflow-x: hidden;
}

.coverPage {
    position: relative;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.coverBackground {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    background: #f0f0f0;  /* 占位符背景 */
}

.coverContent {
    position: relative;
    z-index: 1;
    text-align: center;
}

.coverTitle {
    font-size: 64px;
    font-weight: 700;
    margin: 20px 0;
}

.coverSubtitle {
    font-size: 24px;
    opacity: 0.8;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 60px 40px;
}

.sectionTitle {
    font-size: 48px;
    margin-bottom: 40px;
}

.imageBox {
    position: relative;
    width: 100%;
    height: 500px;
    background: #f0f0f0;  /* 占位符背景 */
    margin: 40px 0;
}

@media (max-width: 768px) {
    .coverTitle {
        font-size: 36px;
    }

    .container {
        padding: 40px 20px;
    }
}
```

---

### Step 3: 创建图片文件夹

**创建文件夹**:
```
public/assets/reports/2025-12-china-market/
```

**文件夹内应该包含** (用户后续准备):
- `cover.jpg` - 列表页封面图
- `banner.jpg` - 轮播背景图
- `01-封面主视觉.jpg` - 详情页封面背景
- `02-市场规模数据图.png` - 数据图表
- 其他报告内需要的图片...

---

### 完成!

现在用户可以:
1. 访问 `/reportsList` - 看到新报告卡片(带灰色占位符)
2. 点击卡片访问 `/reports/2025-12-china-market` - 查看报告详情
3. 根据代码注释准备图片,放入文件夹后图片自动显示

---

## 🎯 总结

### 核心要点

1. **三个步骤缺一不可**: 数据注册 → 组件创建 → 图片文件夹
2. **slug 必须一致**: 在数据、组件文件夹、图片文件夹三处保持相同
3. **组件必须独立**: 使用 `'use client'` 和 `export default`
4. **图片使用占位符**: 灰色背景容器 + 清晰注释说明

### 开发自由度

- ✅ **完全自由**: 报告内容结构、样式设计、交互效果
- ⚠️ **必须遵守**: 文件结构、命名规范、导出方式、图片组件

### 最佳实践

- 使用描述性的文件名和注释
- 为每个图片提供尺寸建议
- 使用 CSS Modules 隔离样式
- 移动端适配使用媒体查询
- 保持代码整洁和注释清晰

---

## 📞 需要帮助?

如果遇到问题:
1. 先检查"验证清单"
2. 查看"常见问题排查"
3. 对照"完整示例"检查代码
4. 确认所有文件路径和命名一致

---

**版本**: v1.0
**最后更新**: 2025-12-09
**适用于**: Next.js 16.x + TypeScript
